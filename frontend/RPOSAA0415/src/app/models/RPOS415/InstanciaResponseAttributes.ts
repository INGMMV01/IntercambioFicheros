export interface IInstanciaResponseAttributes {
    nombre: string;   // Nombre de la instancia PMA
    valor: string;    // Código de la instancia PMA
    orden: number;    // Orden para el listado
}
